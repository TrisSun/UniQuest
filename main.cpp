// ============================================================
// main.cpp
// 程序入口。整个系统的逻辑都封装在 Guild 类里,这里只需
// 创建一个 Guild 对象并调用它的 run() 即可启动。
// ============================================================
#include "Guild.h"

int main() {
    Guild guild;    // 创建公会对象(构造时为未登录状态)
    guild.run();    // 启动主循环:加载数据 -> 显示菜单 -> 处理操作
    return 0;
}
